<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include layout="@layout/toolbar_main" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/list_preferred_item_padding">

        <TextView
            android:id="@+id/apollo_activity_search_text_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center"
            android:layout_margin="40dp"
            android:textColor="@color/app_text_highlight"
            android:textSize="@dimen/text_size_large"
            android:textStyle="bold"
            android:visibility="gone" />

        <GridView
            android:id="@+id/apollo_activity_search_grid_base"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:cacheColorHint="@color/transparent"
            android:drawSelectorOnTop="true"
            android:fadingEdge="vertical"
            android:fastScrollEnabled="true"
            android:horizontalSpacing="@dimen/grid_item_spacing"
            android:scrollbarStyle="outsideOverlay"
            android:scrollbars="vertical"
            android:verticalSpacing="@dimen/grid_item_spacing" />

    </FrameLayout>
</LinearLayout>
